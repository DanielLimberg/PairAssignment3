---
title: "Presentation Pair Assignment"
author: "by Daniel Limberg & Lukas Müller"
subtitle: "Investing on an Odd Relationship"
output: beamer_presentation
---

```{r, echo=FALSE, message=FALSE, include=FALSE, cache=FALSE}
# Descriptive & Inferential Statistics
library(stargazer)
library(Rmisc)
library(ggplot2)
library(magrittr)
library(grid)
library(xtable)

# Set Working Directory
try(setwd("/Users/Lukas/Documents/Git/PairAssignment3/"),silent=TRUE)
try(setwd("C:/Users/Dani/Documents/GitHub2/PairAssignment3/"),silent=TRUE)
getwd()


# Dynamical Link to first R script file
source("PairAssignment3_Code/PairAssignment3_Variables_LagsPercChanges.R")
```

## Overview: The four 'W's'

* **W**hat are we doing?
* **W**hy are we interested in it?
* **W**hich methods and covariates?
* **W**ow: Results!

## Overview

* **What are we doing?**
* Why are we interested in it?
* Which methods and covariates?
* Wow: Results!

## Research Question and Hypothese

*Are there different levels of statistical significance for the effect of a nation's GDP growth on the growth rates of National Stock Markets Indices?*

* H~1~: There are no statistically significant effects of GDP growth on the growth rates of National Stock Market Indices.

## Investing on Effect of GDP Growth...
*(ships=GDP Growth!)* ![](/Users/Lukas/Documents/Git/PairAssignment3/port-labor.jpg)

## ...on the Growth Rate of four National Stock Indices.

**Dependent Variables** | Scope
 -------------------- | -----------------------
 DAX (Germany)        | 30 major Companies
 CAC (France)         | 40 highest Market Caps
 FTSE (Great Britain) | 100 Companies
 NIKKEI (Japan)       | 225 Equities

## The four National Indices 
(aka the four Dependent Variables)

```{r, echo=FALSE, message=FALSE, include=FALSE, cache=FALSE, warning=FALSE}
### Germany
p1 <- ggplot(data = merge10, aes(x = Date, y = DAX.Close.change)) + geom_line() + scale_x_continuous(breaks=seq(1991.1, 2015.4, 10)) + ggtitle("Germany | DAX") + theme(plot.title = element_text(size=14)) + labs(x = "Quarters", y = "Av. Closing Value") + theme(legend.position="none") + theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8))
### Japan
p2 <- ggplot(data = merge10, aes(x = Date, y = NIK.Close.change)) + geom_line() + scale_x_continuous(breaks=seq(1991.1, 2015.4, 10)) + ggtitle("Japan | NIKKEI") + theme(plot.title = element_text(size=14)) + labs(x = "Quarters", y = "Av. Closing Value") + theme(legend.position="none") + theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8))
### France
p3 <- ggplot(data = merge10, aes(x = Date, y = CAC.Close.change)) + geom_line() + scale_x_continuous(breaks=seq(1991.1, 2015.4, 10)) + ggtitle("France | CAC40") + theme(plot.title = element_text(size=14)) + labs(x = "Quarters", y = "Av. Closing Value") + theme(legend.position="none") + theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8))
### Great Britain
p4 <- ggplot(data = merge10, aes(x = Date, y = FTSE.Close.change)) + geom_line() + scale_x_continuous(breaks=seq(1991.1, 2015.4, 10)) + ggtitle("Great Britain | FTSE") + theme(plot.title = element_text(size=14)) + labs(x = "Quarters", y = "Av. Closing Value") + theme(legend.position="none") + theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8))
```

```{r, echo=FALSE, warning=FALSE}
multiplot(p1, p2, p3, p4, cols = 2)
```

## Joint Distribution DVs and key IV

```{r, echo=FALSE, message=FALSE, include=FALSE, cache=FALSE, warning=FALSE}
##DEU
p15 <- ggplot(merge10, aes(x = L.DEU.GDP, y = DAX.Close.change)) + 
  geom_point() + 
  geom_smooth() +
  ggtitle("Germany") +
  xlab("GDP Growth") +
  ylab("DAX Growth Rate in %") +
  theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8)) +
  theme(plot.title = element_text(size=14)) 

## JPN
p16 <- ggplot(merge10, aes(x = L.JPN.GDP, y = NIK.Close.change)) + 
  geom_point() + 
  geom_smooth() +
  ggtitle("Japan") +
  xlab("GDP Growth") +
  ylab("Nikkei Growth Rate in %") +
  theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8)) +
  theme(plot.title = element_text(size=14)) 

## FRA
p17 <- ggplot(merge10, aes(x = L.FRA.GDP, y = CAC.Close.change)) + 
  geom_point() + 
  geom_smooth() +
  ggtitle("France") +
  xlab("GDP Growth") +
  ylab("CAC Growth Rate in %") +
  theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8)) +
  theme(plot.title = element_text(size=14)) 

## GBR
p18 <- ggplot(merge10, aes(x = L.GBR.GDP, y = FTSE.Close.change)) + 
  geom_point() + 
  geom_smooth() +
  ggtitle("Great Britain") +
  xlab("GDP Growth") +
  ylab("FTSE Growth Rate in %") +
  theme(axis.text=element_text(size=8),
        axis.title=element_text(size=8)) +
  theme(plot.title = element_text(size=14)) 
```

```{r, echo=FALSE, message=FALSE, include=TRUE, cache=FALSE, warning=FALSE}
multiplot(p15, p16, p17, p18, cols = 2)
```

## Overview

* What are we doing?
* **Why are we interested in it?**
* Which methods and covariates?
* Wow: Results!

## Good Data Quality

* Daily data for all National Stock Market Indices 
* Easy to aggregate on quarterly basis

## Big Picture

* Are National Stock Markets detached from GDP growth?
* If they are - are markets too far off from the 'real economy'?

## Overview

* What are we doing?
* Why are we interested in it?
* **What methods and covariates?**
* Wow: Results!

## Covariates

* We (can) add to the kitchen sink if we want  
* All Covariates lagged by one Quarter  

  * Covariates
      + GDP Growth USA
      + ECB Main Refinancing Operations
      + ECB Deposit Facility
      + West Texas Intermediate (WTI)
      + Brent Crude

  * Country-Specific Covariates
      + GDP Growth
      + Unemployment
      + Private Consumption

## Methods

* OLS
* (Interactions?)
* (use logs?)

## Overview

* What are we doing?
* Why are we interested in it?
* Which methods and covariates?
* **Wow: Results!**

## Results